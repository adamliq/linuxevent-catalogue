[
  {
    "field": "all",
    "used_in": "Both",
    "format": "keyword (no value)",
    "explanation": "Wildcard: matches any subject (as a subject attribute) or any object (as an object attribute)."
  },
  {
    "field": "dir",
    "used_in": "Both",
    "format": "path",
    "explanation": "Directory path. As a subject attribute, matches the process's current working directory; as an object attribute, matches the directory the accessed file is under. Also accepts the built-in aliases execdirs, systemdirs, and untrusted."
  },
  {
    "field": "ftype",
    "used_in": "Both",
    "format": "MIME type string",
    "explanation": "MIME type of the file, as fapolicyd's magic-based classifier determines it (e.g. application/x-executable, text/x-shellscript). Check a file's type with fapolicyd-cli --ftype."
  },
  {
    "field": "trust",
    "used_in": "Both",
    "format": "boolean (0 or 1)",
    "explanation": "Whether the file must be present in the trust database: 1 = trusted (verified against the RPM database or /etc/fapolicyd/fapolicyd.trust), 0 = explicitly not required to be trusted."
  },
  {
    "field": "auid",
    "used_in": "Subject",
    "format": "numeric",
    "explanation": "Audit login UID (loginuid) of the process — the same immutable auid used throughout the kernel audit subsystem. 4294967295 means unset (no login session)."
  },
  {
    "field": "uid",
    "used_in": "Subject",
    "format": "numeric or account name",
    "explanation": "Real user ID (or account name) of the process."
  },
  {
    "field": "gid",
    "used_in": "Subject",
    "format": "numeric or group name",
    "explanation": "Real group ID (or group name) of the process."
  },
  {
    "field": "sessionid",
    "used_in": "Subject",
    "format": "numeric",
    "explanation": "Audit session ID of the process."
  },
  {
    "field": "pid",
    "used_in": "Subject",
    "format": "numeric",
    "explanation": "Process ID."
  },
  {
    "field": "ppid",
    "used_in": "Subject",
    "format": "numeric",
    "explanation": "Parent process ID — e.g. used to identify kernel threads such as nfsd (ppid=2, since kernel threads have no executable of their own)."
  },
  {
    "field": "comm",
    "used_in": "Subject",
    "format": "string",
    "explanation": "Short command name of the process (as in /proc/<pid>/comm), e.g. dnf, nfsd."
  },
  {
    "field": "exe",
    "used_in": "Subject",
    "format": "path (glob: supported)",
    "explanation": "Full path to the process's executable. Supports a glob: prefix for fnmatch-style pattern matching."
  },
  {
    "field": "pattern",
    "used_in": "Subject",
    "format": "enum",
    "explanation": "Access pattern the kernel identified: normal, ld_so (dynamic-loader-mediated access), ld_preload, or static."
  },
  {
    "field": "path",
    "used_in": "Object",
    "format": "path (glob: supported)",
    "explanation": "Full path of the file being accessed. Supports a glob: prefix for fnmatch-style pattern matching."
  },
  {
    "field": "device",
    "used_in": "Object",
    "format": "path",
    "explanation": "Device special file path, e.g. /dev/sda."
  },
  {
    "field": "sha256hash",
    "used_in": "Object",
    "format": "hex digest (SHA-256 or SHA-512)",
    "explanation": "Hash of the file's contents, lowercase, no 0x prefix — matches a specific file by content rather than by path."
  }
]
